/**
 * True Oath Five: Workspace Switcher (The Lobby)
 * Reads the user's multi-tenant profile and displays buttons.
 */
function renderWorkspaceLobby(userProfile) {
    console.log("Rendering Company Lobby...");
    userProfile.companies.forEach(company => {
        console.log(Creating button for  as );
        // When clicked, triggers redirect:
        // window.location.href = ../index.html?company_id=;
    });
}
